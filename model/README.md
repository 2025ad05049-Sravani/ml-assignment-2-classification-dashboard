# Model implementations

The five `.py` files in this directory are the inspectable source implementations required by the assignment. The `.joblib` files are serialized trained estimators used by Streamlit for fast loading.
